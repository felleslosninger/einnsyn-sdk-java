name: Dispatch Codegen

on:
  workflow_dispatch: {}
  push:
    branches: [main]

jobs:
  dispatch-codegen:
    runs-on: ubuntu-latest

    env:
      commit_message: ""
      pr_body: ""

    steps:
      - name: Get token
        uses: actions/create-github-app-token@1b10c78c7865c340bc4f6099eb2f838309f1e8c3 # v3.1.1
        id: app-token
        with:
          app-id: ${{ secrets.EIN_CODEGEN_APP_ID }}
          private-key: ${{ secrets.EIN_CODEGEN_APP_PRIVATE_KEY }}
          owner: ${{ github.repository_owner }}
          skip-token-revoke: true
          repositories: |
            einnsyn-codegen

      - name: Build release metadata
        run: |
          if [ "${{ github.event_name }}" = "push" ] && [[ "${{ github.event.head_commit.message }}" == *"Merge pull request"* ]]; then
            PR_NUMBER=$(echo "${{ github.event.head_commit.message }}" | grep -oP '(?<=#)\d+')

            echo "pr_body=Triggered from ${{ github.repository }} [#${PR_NUMBER}](https://github.com/${{ github.repository }}/pull/${PR_NUMBER})" >> "$GITHUB_ENV"
            echo "commit_message=fix: sync codegen from ${{ github.repository }}#${PR_NUMBER}" >> "$GITHUB_ENV"
          else
            echo "pr_body=Triggered from ${{ github.repository }}" >> "$GITHUB_ENV"
            echo "commit_message=fix: sync codegen from ${{ github.repository }}" >> "$GITHUB_ENV"
          fi

      - name: Dispatch workflow
        uses: peter-evans/repository-dispatch@28959ce8df70de7be546dd1250a005dd32156697 # v4.0.1
        with:
          token: ${{ steps.app-token.outputs.token }}
          repository: felleslosninger/einnsyn-codegen
          event-type: codegen
          client-payload: |
            {
              "commit_message": "${{ env.commit_message }}",
              "pr_body": "${{ env.pr_body }}",
              "type": "sdk-java"
            }
